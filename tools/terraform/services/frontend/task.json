[
    {
        "name": "${service_name}",
        "image": "${image_url}",
        "essential": true,
        "portMappings": [
            {
                "protocol": "tcp",
                "containerPort": 80
            }
        ]
    }
]